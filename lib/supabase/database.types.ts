// Este archivo será autogenerado con: pnpm db:types
// Mantener vacío hasta primera sincronización con Supabase

export type Json =
  | string
  | number
  | boolean
  | null
  | { [key: string]: Json | undefined }
  | Json[]

export interface Database {
  // Será generado automáticamente
}
